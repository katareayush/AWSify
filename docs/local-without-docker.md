# Local Setup Without Docker

Docker is optional for local development. If you do not use Docker, run PostgreSQL and Redis with your preferred local setup, then set `DATABASE_URL` and `REDIS_URL` in `.env`.

Use `.env.example` as the only checklist for required environment variables.
