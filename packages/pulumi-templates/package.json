{
  "name": "@awsify/pulumi-templates",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@awsify/deployment-schemas": "workspace:*",
    "@pulumi/aws": "^6.66.2",
    "@pulumi/pulumi": "^3.142.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
